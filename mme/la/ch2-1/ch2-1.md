# 斐波那契数列

内容提要：根据数列的递推关系式计算数列的通项。

## 一、问题描述

设有一对兔子出生一个月后开始繁殖，每个月出生一对小兔子。小兔子一个月后也开始繁殖，每个月又出生一对小兔子。假设兔子只有繁殖，没有死亡，求第 $k$ 个月的时候一共有多少对兔子？

## 二、建立模型

设第 $k$ 个月开始的时候，一共有 $F_k$ 对兔子。则根据题目假设，有
前几个月的兔子数量（单位：对）

|月份 |0|1|2|3|4|5 | $\cdots$ |
|---|---|---|---|---|---|---|---|
|兔子数量 |1|1|2|3|5|8| $\cdots$ |

写成递推关系式，可得
$$
F_0=1,\quad F_1=1,\quad F_{k+2}=F_{k+1}+F_k,\quad k=0,1,2,\cdots.  
$$

## 三、求解模型

 ### （一）矩阵的对角化方法

数列的递推关系式可以写成如下形式
$$
\begin{bmatrix} F_{k+1} \\ F_{k+2} \end{bmatrix} 
=
\begin{bmatrix} 0&1 \\ 1&1 \end{bmatrix} 
\begin{bmatrix} F_k \\ F_{k+1} \end{bmatrix}.  
$$

由此可得
$$
\begin{bmatrix} F_{k+1} \\ F_{k+2} \end{bmatrix} 
=
\begin{bmatrix} 0&1 \\ 1&1 \end{bmatrix} 
\begin{bmatrix} F_k \\ F_{k+1} \end{bmatrix}
=
\begin{bmatrix} 0&1 \\ 1&1 \end{bmatrix}^2
\begin{bmatrix} F_{k-1} \\ F_k \end{bmatrix}
= \cdots =
\begin{bmatrix} 0&1 \\ 1&1 \end{bmatrix}^{k+1}
\begin{bmatrix} F_{0} \\ F_1 \end{bmatrix}. 
$$

因此问题转化成计算矩阵 $A$ 的 $n$ 次幂，其中 
$$
A = \begin{bmatrix} 0&1 \\ 1&1 \end{bmatrix}.  
$$
计算矩阵 $A$ 的特征值和特征向量，可得 特征值为 
$$
\lambda_1 = \frac{1-\sqrt{5}}{2}, \,\,\, \lambda_2 = \frac{1+\sqrt{5}}{2}.
$$
相应的特征向量为
$$
\xi_1 = \begin{bmatrix} \sqrt{5}+1 \\ -2 \end{bmatrix}, \quad
\xi_2 = \begin{bmatrix} \sqrt{5}-1 \\ 2 \end{bmatrix}. 
$$
设矩阵 
$$
P = \begin{bmatrix} \xi_1 & \xi_2 \end{bmatrix}
= \begin{bmatrix} \sqrt{5}+1 & \sqrt{5}-1 \\ -2 & 2 \end{bmatrix}, \quad
B = \begin{bmatrix} \lambda_1 & 0 \\ 0 & \lambda_2 \end{bmatrix}
= \begin{bmatrix} \frac{1-\sqrt{5}}{2} & 0 \\ 0 & \frac{1+\sqrt{5}}{2} \end{bmatrix}. 
$$
则有 
$$
P^{-1}AP = B.
$$
由此可得 $A = PBP^{-1}$, 以及 
$$
A^n = (PBP^{-1})^n = (PBP^{-1})(PBP^{-1})\cdots (PBP^{-1}) = PB^nP^{-1}. 
$$

### （二）差分方程降阶方法

设从递推关系式
$$
F_{k+2}=F_{k+1}+F_k 
$$
可以得到 
$$
F_{k+2} - a F_{k+1} =b(F_{k+1} - aF_k),   
$$
其中 $a,b$ 待定。比较两式可得
$$
a+b=1,\quad ab=-1.
$$
由此求出 $a,b$. 

### （三）回答问题

通过矩阵的对角化方法，可以求得数列的通项，也就是第 $k$ 个月的兔子数量为
$$
F_k = \left(\frac{1}{2} -\frac{\sqrt{5}}{10} \right)  \left( \frac{1-\sqrt{5}}{2} \right)^k 
+  \left( \frac{1}{2} +\frac{\sqrt{5}}{10} \right)  \left( \frac{1+\sqrt{5}}{2} \right)^k. 
$$

### （四）代码
```python
import numpy as np

N=21
myF=np.zeros(N,dtype=int)
myF[0]=1
myF[1]=1
for k in range(2,N):
    myF[k]=myF[k-1]+myF[k-2]

print(myF)
```

```python
import sympy as sp

sp.var('k',positive=True, integer=True)
a = sp.Matrix([[0, 1], [1, 1]])
val = a.eigenvals()
vec = a.eigenvects()
P, D = a.diagonalize()
ak = P @ (D ** k) @ (P.inv())
F = ak @ sp.Matrix([1, 1])
s = sp.simplify(F[0])
print(s) 
sm = []
for i in range(20):
    sm.append(int(s.subs(k, i)))
print(sm)
```

```python

```


## 四、附加问题

1. 求差分方程 $x_{n+2}-x_{n+1}-2x_n=0, x_0=x_1=-2$ 的解。用不同方法求出通项，并编程验证。

2. 设 $F_n$ 为第 $n$ 个斐波那契数，设 $S=\sum\limits_{n=1}^{\infty} \frac{1}{F_n^2}$. 

(1).  用 $S$ 表示 $\sum\limits_{n=1}^{\infty} \frac{1}{(F_{n+2}F_{n+1}F_n)^2}$. 

(2).  用 $S$ 表示 $\sum\limits_{n=1}^{\infty} \frac{1}{(F_{n+2}F_n)^2}$. 

## 参考文献

1. 司守奎,孙玺菁. **Python数学建模算法与应用**, 国防工业出版社. 2022年1月第1版. 

